1.

// Пример максимально раннего связывания - инициализация переменной step в конструкторе.
// Такой выбор был сделан, т.к. переменную необходимо инициализировать один раз при создании 
// объекта и использовать как константу:

class NativeDictionary<T> {
    final public int step;
   
    public NativeDictionary() {
        step = 3;
    }

2.

// Пример связывания во время компиляции кода с помощью константы.
// Выбран данный тип связывания, т.к. для нескольких циклов используется один и тот же шаг,
// а так же, для более удобного изменения шага, если потребуется 
// (будет достаточно изменить значение константы, а не шаг в каждом цикле):

final int STEP_FOR = 2;
for (int i = 0; i < oksana.length - 1; i += STEP_FOR)

  
3.

// Пример связывания во время выполнения программы:
 
values[find(key)]=value;

// индекс массива values для записи переменной value - результат вызова функции find;
// выбрано данное связывание, т.к.номер индекса зависит от выполнения условий прописанных в функции
// find;
